import type { NetworkContent } from '~~/types/content'

export const networkContent: NetworkContent = {
  status: 'draft',
  title: 'Our Network',
  intro:
    'The distribution channels Binzomah\'s network reaches. Full page content is pending stakeholder approval.',
}
